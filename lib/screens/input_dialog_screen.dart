import 'package:flutter/material.dart';

class InputDialogScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white.withOpacity(0.5),
      body: Center(
        child: Text('Mnemonic field'),
      ),
    );
  }
}
